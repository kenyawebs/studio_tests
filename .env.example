# This file is an example. Create a .env.local file to use these variables.

# =================================================================
# Required for Firebase
# =================================================================
# Get these from your Firebase project settings > General > Your apps > Firebase SDK snippet > Config
NEXT_PUBLIC_FIREBASE_API_KEY="your-api-key-here"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-sender-id"
NEXT_PUBLIC_FIREBASE_APP_ID="your-app-id"

# =================================================================
# Required for Genkit with Vertex AI
# =================================================================
# By using Vertex AI, you no longer need the GOOGLE_AI_API_KEY.
# Genkit will automatically use your authenticated Google Cloud project.
# You can set these explicitly, or they will be inferred if you are logged
# in via the gcloud CLI.
#
# Your Google Cloud Project ID (can be found on the Google Cloud Console dashboard)
# GCLOUD_PROJECT="your-gcloud-project-id"
#
# The region for your Vertex AI services (e.g., "us-central1")
# GCLOUD_LOCATION="your-gcloud-location"
