# This file is an example. Create a .env.local file to use these values.

# Firebase Project Credentials
# These are required for the app to function.
# Get them from your Firebase project settings (web app).
NEXT_PUBLIC_FIREBASE_API_KEY="your-api-key-here"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-sender-id"
NEXT_PUBLIC_FIREBASE_APP_ID="your-app-id"

# Google Cloud Project Configuration (for Vertex AI)
# These are used for server-side AI features and are more secure than API keys for backend services.
# The project ID and location of your Google Cloud project.
GCLOUD_PROJECT="your-gcloud-project-id"
GCLOUD_LOCATION="us-central1"

# Google Maps API Key
# This is required for the interactive maps on the Events and Volunteering pages.
# Get this from the Google Cloud Console (APIs & Services > Credentials).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key"
